int main (int c) {
    float elem;

    read(elem);

    if(elem == 0) {
        int a;
        write("zero\n");
    } else {
        int b;
        write(" not zero\n");
    }

    int c;
    read(elem);
    read(elem);
    read(elem);
}
